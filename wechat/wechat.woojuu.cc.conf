
server
	{
		listen       80;
		server_name wechat.woojuu.cc;
		index index.html index.htm;
		root  /home/wwwroot/wechat.woojuu.cc/app;

		include none.conf;
		location / { try_files $uri @woojuu-wechat; }
		location @woojuu-chat {
				include uwsgi_params;
				uwsgi_pass unix:/tmp/uwsgi_woojuu_wechat.sock;
		}
        location /static {
                alias /home/wwwroot/woojuu.cc/app/static;
        }

		location ~ .*\.(gif|jpg|jpeg|png|bmp|swf)$
			{
				expires      30d;
			}

		location ~ .*\.(js|css)?$
			{
				expires      12h;
			}

		access_log off;
	}
